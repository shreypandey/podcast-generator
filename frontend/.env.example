# Run the whole UI against the in-browser mock backend (no server needed):
#   VITE_USE_MOCK=1 npm run dev
VITE_USE_MOCK=1

# In real mode (VITE_USE_MOCK unset), dev proxies /api to this target:
# VITE_API_TARGET=http://localhost:8000
